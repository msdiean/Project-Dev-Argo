apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    role: frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}   # 80
      targetPort: 80
      protocol: TCP
